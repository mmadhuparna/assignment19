<!DOCTYPE html>
<html lang="en">

<head>
    <title>
        <%= title %>
    </title>
    <!--include header part-->
    <% include ../shared/head %>

    <!--applying internal styles-->
    <style>
        body{
            background-color: rgb(30, 66, 44);
                color: rgb(214, 208, 208);
        }
        .page-header{
            color: rgb(245, 239, 239);
        }
        .label{
            padding-left: 20px;
            padding-right: 20px;
        }
        .bg{
            margin-top: 10%;
        }
        .btn-primary{
            letter-spacing: 1px;
        }
        button{
            background-color: transparent;
            border: none;
        }
        span{
            font-size: 18px;
            letter-spacing: 1px;
            text-transform:capitalize;
            color: rgb(146, 142, 142);
        }
    </style>
</head>

<body >
    <!--main body starts-->
    <div class="container">
            
        <div class="row">
            <div class="bg col-md-4 col-md-offset-4">
                <span style="color: #000;">Hello!</span>
                <span style="color: #000;" ><%= user.username %></span>
                <h3 style="color: #000;">You are successfully logged in</h3>
                <div class="page-header">
                    <h3>Your login Details</h3>
                </div>
                                
                <form id="" method="POST" >
                    <div class="form-group">
                        <label >ID:&nbsp</label>
                        <span ><%= user.id %></span>
                    </div>

                    <div class="form-group">
                        <label >Username:&nbsp</label>
                         <span ><%= user.username %></span>
                    </div>
                        
                    <div class="form-group">
                        <label >First Name:&nbsp</label>
                        <span ><%= user.firstname %></span>
                    </div>

                    <div class="form-group">
                        <label >Last Name:&nbsp</label>
                        <span ><%= user.lastname %></span>
                    </div>
                        
                    <div class="form-group">
                        <label >Email:&nbsp</label>
                        <span style="text-transform: lowercase"><%= user.email %></span>
                    </div>

                    <div class="form-group">
                        <button type="submit"><a href="/profile/"  class="btn btn-primary">Profile-page</a></button>
                        <a href="/" class="btn btn-primary">logout</a>
                    </div>
                </form>
            </div>
        </div>    
    </div>                   
</body>
</html>